import { ImagesObservations } from './images-observations';

describe('ImagesObservations', () => {
  it('should create an instance', () => {
    expect(new ImagesObservations()).toBeTruthy();
  });
});
